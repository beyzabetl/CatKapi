rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All products, categories, and site settings are publicly viewable by visitors
    // and updatable via the admin management console
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
